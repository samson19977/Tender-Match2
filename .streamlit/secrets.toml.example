# Copy this file to .streamlit/secrets.toml for local development.
# On Streamlit Cloud, paste the same key/value pairs in
# App Settings → Secrets — do NOT commit secrets.toml to git.

GROQ_API_KEY = "gsk_..."
