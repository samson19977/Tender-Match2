# For local development without Streamlit secrets.
# Copy to .env, fill in your key, then either:
#   - load it manually: export $(cat .env | xargs)
#   - or install python-dotenv and call load_dotenv() in app.py
#
# .env is listed in .gitignore — never commit it.

GROQ_API_KEY=gsk_...
